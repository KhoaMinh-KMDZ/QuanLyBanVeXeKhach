<Window x:Class="QuanLyBanVeXeKhach.Views.KetNoiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Cấu Hình Kết Nối CSDL" Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CẤU HÌNH SQL SERVER" FontSize="20" FontWeight="Bold" Foreground="#1976D2" HorizontalAlignment="Center" Margin="0,5,0,15"/>
        
        <TextBlock Grid.Row="1" Text="Nhập Tên Server của máy tính này:" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Gray"/>
        <TextBox Grid.Row="2" x:Name="txtServerName" VerticalAlignment="Top" 
                 materialDesign:HintAssist.Hint="Ví dụ: . hoặc .\SQLEXPRESS" 
                 Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontSize="16" FontWeight="SemiBold"/>

        <Button Grid.Row="4" Content="KIỂM TRA &amp; KẾT NỐI" Click="BtnConnect_Click" Height="45" FontSize="14" FontWeight="Bold" Margin="0,15,0,0"/>
    </Grid>
</Window>
